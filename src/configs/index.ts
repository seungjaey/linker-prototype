export const TARGET_BASE_HOST = process.env.NEXT_PUBLIC_TARGET_BASE_URL || '';

export const IOS_PREFIX = process.env.NEXT_PUBLIC_IOS_PREFIX || '';
export const AOS_PREFIX = process.env.NEXT_PUBLIC_AOS_PREFIX || '';

export default {
  TARGET_BASE_HOST,
  IOS_PREFIX,
  AOS_PREFIX,
};
